{
  "version": 2,
  "buildCommand": "npx prisma migrate deploy --schema=./prisma/schema.prisma && npx prisma generate --schema=./prisma/schema.prisma && npm run build",
  "outputDirectory": "apps/web/dist"
}
